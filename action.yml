name: "nodesafe scan"
description: "Scan ComfyUI custom_nodes (and other node-based plugins) for malicious code"
author: "nodesafe contributors"
branding:
  icon: "shield"
  color: "purple"

inputs:
  path:
    description: "Path to scan (file or directory)"
    required: false
    default: "custom_nodes/"
  fail-on:
    description: "Severity threshold for non-zero exit (none|suspicious|malicious)"
    required: false
    default: "suspicious"
  format:
    description: "Output format (json|markdown). SARIF planned for v0.5."
    required: false
    default: "markdown"
  python-version:
    description: "Python version to use"
    required: false
    default: "3.11"

runs:
  using: "composite"
  steps:
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ inputs.python-version }}

    - name: Install nodesafe
      shell: bash
      run: pip install nodesafe

    - name: Run nodesafe scan
      shell: bash
      run: |
        nodesafe scan "${{ inputs.path }}" \
          --fail-on "${{ inputs.fail-on }}" \
          --format "${{ inputs.format }}"
